version: '3.8'

services:
  alert-triage:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alert-triage
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - LOG_LEVEL=INFO
    ports:
      - "8000:8000"
    restart: unless-stopped
